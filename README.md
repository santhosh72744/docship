📦 DocShip: International Document Courier AppDocShip is a full-stack web application facilitating the complete workflow for sending documents from the USA to India via couriers like FedEx/UPS.1. ⚙️ Core Workflow (4 Steps)The application guides the user through four sequential steps:Step 1: EstimationStep 2: Address CollectionStep 3: PaymentStep 4: Shipping Label2. ✨ Key Features (Point-wise Breakdown)2.1 Estimation & PricingInputs: Requires Shipping Type (Document), Source (USA), Destination (India), 5-digit U.S. ZIP, 6-digit Indian PIN, and Courier Selection (FedEx/UPS).Validation: Backend verifies ZIP/PIN existence and supported PIN ranges.Output: Returns Price (INR + USD) and guaranteed Delivery Days (FedEx: 5 days; UPS: 7 days).2.2 Address HandlingAuto-Parser: Enables users to paste a full address (multi-line or single-line).Extraction: System automatically extracts and populates: Name, Phone, Address Line 1, City, State, ZIP/PIN, and Aadhaar (for Destination).Manual Entry: All fields in the CheckoutForm remain editable for manual input or correction.2.3 State PersistenceMechanism: Uses Local Storage to mirror the entire application state.Key: All data is stored under localStorage['docshipData'].Function: Allows the user to refresh the page or close the browser without losing progress.2.4 Payment & Label GenerationPayment: Collects simulated card details (Number, Name, Expiry, CVV).API Calls: Triggers /api/save-shipment (generates Tracking Number) and /api/save-payment (generates Payment Reference ID).Final Output: Displays the complete Shipping Label with all addresses, cost, Tracking Number, and Payment Reference.Reset: The dedicated Reset button clears all state and local storage, returning the app to Step 1.3. 🏗️ Tech Stack SummaryFrontend: React (Functional Components, Hooks), Local Storage (State Persistence), CSS.Backend: Node.js + Express (API Server).Data Storage: Simulated via JSON Files (shipments.json, payments.json).4. 🌐 API EndpointsMethodEndpointPurposePOST/api/estimateReturns price and delivery days.POST/api/save-shipmentStores shipment data; returns Tracking Number.` POST**/api/save-payment\Stores payment data; returns Payment Reference ID.5. 🏃 Running the ProjectBoth services must be running simultaneously:Backend Start (First):Bashcd backend
npm install
node server.js
Listens at http://localhost:5000.Frontend Start (Second):Bashcd frontend
npm install
npm start
Opens app at http://localhost:3000.
